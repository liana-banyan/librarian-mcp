"""
Librarian MCP — Pre-curated canonical memory + prose/code provenance for any AI coding client.

Two MCP tools:
  librarian_context   — loads canonical memory packet for the current project
  prose_provenance    — drift detection between two versions of any document
"""

__version__ = "0.1.0"
